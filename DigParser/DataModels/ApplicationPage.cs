namespace DIGStatus
{
	/// <summary>
	/// A page of the application
	/// </summary>
    public enum ApplicationPage
    {
		// the initial user page
		User = 0,

		// the initial dig games page
		DigGame = 1,

		// the initial main program page
		MainPage = 2,

		// the initial page with column chart
		ChartPage = 3, 
    }
}
